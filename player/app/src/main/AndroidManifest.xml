<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required for SSID lookup on Android 8.1+ via WifiManager.connectionInfo. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Self-update: needed to ask Android's PackageInstaller to swap
         in a new APK we just downloaded. The OS still gates the
         actual install on the per-app "Allow from this source" toggle
         the first time it's used; we send the user to that settings
         page if it's not granted. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- TV / signage support.
         The same APK installs on retail tablets (touch) and on Android TV /
         Fire TV / signage boxes (D-pad only). Marking touch as not-required
         lets the TV launcher pick the APK up; declaring leanback as not-
         required keeps tablet installs unaffected. The runtime branch
         (InputMode.isTvLike) flips the unlock UX to up-up-down-down. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".ScreensApp"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Screens"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:banner="@drawable/tv_banner"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.Screens.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Phone / tablet launchers + kiosk HOME mode. -->
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Android TV / Fire TV launchers look for this category. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging receiver. -->
        <service
            android:name=".fcm.ScreensFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Auto-confirm accessibility service (v0.2.7). Presses INSTALL on
             the system update dialog so an unattended screen self-updates
             without anyone tapping. Disabled until an operator enables it in
             Settings → Accessibility; the Device admin screen has a one-tap
             shortcut. BIND_ACCESSIBILITY_SERVICE is the required system
             permission that lets only the OS bind this service. -->
        <service
            android:name=".update.InstallAutoConfirmService"
            android:exported="false"
            android:label="@string/auto_confirm_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/install_auto_confirm_service" />
        </service>

        <!-- FileProvider exposes the downloaded APK to the system
             PackageInstaller as a content:// URI. The authority must
             match Updater.launchInstaller's hard-coded format
             (`<packageName>.fileprovider`). Paths are restricted to
             the updates/ subdir in @xml/file_provider_paths. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
